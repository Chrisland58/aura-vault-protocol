// Global test setup — runs before every test file in the frontend suite.
// Imports the jest-dom custom matchers so assertions like
// `expect(el).toBeInTheDocument()` are available in all tests.
import "@testing-library/jest-dom";
