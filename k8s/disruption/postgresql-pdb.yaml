apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: postgresql-pdb
  namespace: aura-vault
  labels:
    app: postgresql
    component: database
    role: primary
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: postgresql
      role: primary
